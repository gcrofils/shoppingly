%h1 Posts I liked
%ul
  - posts.each do |post|
    %li
      = link_to post.title, post_path(post)
      %span{id: "#{dom_id(post, 'social')}"}
        = render partial: "users/likes", locals: {resource: post}